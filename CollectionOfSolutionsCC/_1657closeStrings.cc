class Soltuion
{
public:
    bool closeStrings(string word1, string word2)
    {
        // 首先，两个字符串必须长度相等，否则不可能是接近的
        int m = word1.size(), n = word2.size();
        if(m != n)
        {
            return false;
        }
        // 否则，二者长度相等，我们可以进一步判断
        
    }
};